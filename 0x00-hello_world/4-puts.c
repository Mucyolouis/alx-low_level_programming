#include <stdio.h>
/**
 * main -  a C program that prints using puts and followed by a new line.
 * Return: 0(Success)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
